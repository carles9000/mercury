

function LoadLang()

	aStr[ 'desconocido' ]				:= "Unknow parameter %1"
	aStr[ 'hello' ]						:= "Hello ! Now is %1"
	aStr[ 'no message' ]				:= "No message"
	aStr[ 'no help' ]					:= "Help don't exist %1"
	aStr[ 'help controller error' ]		:= "Controller wrong. See => help controller  or  /controller"
	aStr[ 'error /' ]					:= "First character can't be / or \"
	aStr[ 'character error .' ]			:= "Character not allowed: . "
	aStr[ 'controller name empty' ]		:= "Controller name controller is empty ex. => controller new MyController"
	aStr[ 'controller not found' ]		:= "Controller file not found: %1"
	aStr[ 'controller deleted' ]		:= "Controller %1 has deleted !"
	aStr[ 'controller deleted error' ]	:= "Controller %1 don't deleted !"
	aStr[ 'controller folder error' ]	:= "Controller folder doen't exist"
	aStr[ 'controller exist renew' ]	:= "%1 exist !. You can force with -renew- param..."
	aStr[ 'controller created' ]		:= "Controller %1 has created!"
	aStr[ 'controller recreated' ]		:= "Controller %1 has recreated!"
	aStr[ 'controller created error' ]	:= "Error while creating controller %1"
	aStr[ 'template not found' ]		:= "Template file not found: %1"
	aStr[ 'show controller' ]			:= "SHOW CONTROLLER %1 <hr> %2 <hr>"
	aStr[ 'unknow parameter' ]			:= "Unknown parameter %1"
	aStr[ 'dir create' ]				:= "Create directory %1"
	aStr[ 'dir create error' ]			:= "Error make directory %1"
	
retu nil